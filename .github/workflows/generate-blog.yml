name: Generate Blog Posts

on:
  schedule:
    # Diario a las 8am UTC — genera 2 artículos (fase inicial, primeros 2 meses)
    # Cuando pases a fase regular: cambia a "0 8 * * 1,3,5" y COUNT a 1
    - cron: '0 8 * * *'
  workflow_dispatch:
    inputs:
      count:
        description: 'Número de artículos a generar'
        default: '2'
        type: string

permissions:
  contents: write

concurrency:
  group: generate-blog
  cancel-in-progress: false

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Generate articles
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          COUNT: ${{ github.event.inputs.count || '2' }}
        run: node scripts/generate-blog.js

      - name: Commit & push
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/blog/ docs/sitemap.xml scripts/blog-state.json || true
          git diff --cached --quiet || git commit -m "chore: generate blog posts [skip ci]"
          git push
